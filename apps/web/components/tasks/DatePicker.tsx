/**
 * The plan refers to this component as `DatePicker`. The implementation
 * lives in `DueDatePicker.tsx`; we re-export it under the plan's name to
 * keep both import paths valid.
 */
export { DueDatePicker as DatePicker } from "./DueDatePicker";
