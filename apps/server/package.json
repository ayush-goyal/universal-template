{
  "name": "@acme/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.node.js",
  "scripts": {
    "build": "tsup",
    "build:cloudflare": "wrangler deploy --dry-run --outdir dist/worker",
    "cf-typegen": "wrangler types --include-runtime false",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "deploy:cloudflare": "wrangler deploy",
    "dev": "pnpm with-env tsx watch src/server.node.ts",
    "dev:cloudflare": "wrangler dev --env-file \"$(cd ../.. && pwd)/.env\"",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "format:fix": "prettier --write . --ignore-path ../../.gitignore",
    "lint": "oxlint --config ../../.oxlintrc.json --type-aware .",
    "lint:fix": "oxlint --config ../../.oxlintrc.json --type-aware --fix .",
    "start": "pnpm with-env node dist/server.node.js",
    "test": "vitest run && vitest run --config vitest.worker.config.ts",
    "typecheck": "tsc --noEmit",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@acme/db": "workspace:*",
    "@hono/node-server": "2.1.1",
    "hono": "4.13.5",
    "tsup": "^8.5.1"
  },
  "devDependencies": {
    "@acme/prettier-config": "workspace:*",
    "@acme/tsconfig": "workspace:*",
    "@acme/vitest-config": "workspace:^",
    "@cloudflare/vitest-plugin": "1.1.3",
    "@types/node": "catalog:",
    "dotenv-cli": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "prettier": "catalog:",
    "tsx": "^4.21.0",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  },
  "prettier": "@acme/prettier-config"
}
