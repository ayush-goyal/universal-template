{
  "name": "@acme/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "workerd": {
        "types": "./dist/src/client.workerd.d.ts",
        "default": "./src/client.workerd.ts"
      },
      "types": "./dist/src/client.node.d.ts",
      "default": "./src/client.node.ts"
    },
    "./worker": {
      "types": "./dist/src/client.workerd.d.ts",
      "default": "./src/client.workerd.ts"
    }
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate",
    "clean": "git clean -xdf .cache .turbo dist node_modules generated",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "format:fix": "prettier --write . --ignore-path ../../.gitignore",
    "lint": "oxlint --config ../../.oxlintrc.json --type-aware .",
    "lint:fix": "oxlint --config ../../.oxlintrc.json --type-aware --fix .",
    "db:migrate": "pnpm with-env prisma migrate dev",
    "db:migrate:prod": "pnpm with-env prisma migrate deploy",
    "db:studio": "pnpm with-env prisma studio",
    "test": "vitest run",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false && tsc --project tsconfig.prisma.json",
    "with-env": "dotenv -e ../../.env --",
    "postinstall": "prisma generate && prisma format"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.7.0",
    "@prisma/adapter-ppg": "7.8.0",
    "@prisma/client": "^7.7.0",
    "minimatch": "^10.1.1",
    "pg": "^8.16.3",
    "prisma": "^7.7.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@acme/prettier-config": "workspace:*",
    "@acme/tsconfig": "workspace:*",
    "@acme/vitest-config": "workspace:^",
    "@types/node": "catalog:",
    "@types/pg": "^8.16.0",
    "dotenv-cli": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "prettier": "@acme/prettier-config"
}
